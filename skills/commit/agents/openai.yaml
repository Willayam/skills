interface:
  display_name: "Commit"
  short_description: "Commit dirty worktrees in coherent chunks"
  default_prompt: "Use /commit to split all current repository changes into logical git commits."

policy:
  allow_implicit_invocation: true
